---
name: publish-ui
description: 启动 Garry Publish 排版工具的本地开发服务器并在浏览器中打开
---

# 启动排版工具

启动 Garry Publish 的本地开发服务器并在浏览器中打开。

## 步骤

1. 在项目根目录运行 `npm run dev`（如果 node_modules 不存在则先运行 `npm install`）
2. 等待 Vite 启动完成，获取本地地址
3. 用 `open` 命令在默认浏览器中打开该地址
4. 告诉用户地址和基本使用方法
